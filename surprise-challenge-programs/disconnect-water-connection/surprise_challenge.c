void supriseChallenge()
{
    // For the right water connection, replace the moving forward after the wall square to the following line
    moveNoSyncMinspeedParam(100, 100, 735, 0.2, 0.2, true, RELDEG);

    // For the left water connection replace the moving forward after the wall square to the following line
    moveNoSyncMinspeedParam(100, 100, 703, 0.2, 0.2, true, RELDEG);
}